/*
 * ┌─ 文件 ────────────────────────────┐
 * │ vite-env.d.ts                     │
 * │ 浏览器 → nginx :8080              │
 * └──────────────────────────────────┘
 *
 * 浏览器打开由 nginx :8080 提供的页面
 * │
 * ▼
 * 【本文件】引用 Vite 客户端类型
 * │
 * └──▶ 让 TypeScript 认识 Vite 注入的类型
 *
 * 一句话：不发请求，也不打开 PostgreSQL。
 */
/// <reference types="vite/client" />

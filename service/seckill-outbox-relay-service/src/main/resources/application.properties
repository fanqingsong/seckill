spring.mvc.pathmatch.matching-strategy=ant_path_matcher
spring.jpa.open-in-view=false
server.port=8087
# H2 / 集成测试只用轮询；Compose 的 prd 会打开 CDC。
seckill.outbox.cdc.enabled=false
seckill.outbox.relay.poll-interval-ms=200
